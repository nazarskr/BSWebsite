import { ChapterizeWorksPipe } from './chapterize-works.pipe';

describe('ChapterizeWorksPipe', () => {
  it('create an instance', () => {
    const pipe = new ChapterizeWorksPipe();
    expect(pipe).toBeTruthy();
  });
});
