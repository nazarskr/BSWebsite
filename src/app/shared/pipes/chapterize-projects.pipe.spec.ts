import { ChapterizeProjectsPipe } from './chapterize-projects.pipe';

describe('ChapterizeProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChapterizeProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
