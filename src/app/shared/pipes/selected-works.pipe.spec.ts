import { SelectedWorksPipe } from './selected-works.pipe';

describe('SelectedWorksPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedWorksPipe();
    expect(pipe).toBeTruthy();
  });
});
